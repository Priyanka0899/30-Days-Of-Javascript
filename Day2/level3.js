// const sen = 'Love is the best thing in this world. Some found their love and some are still looking for their love.';
// const matches = sen.match(/love/gi);
// const count = matches ? matches.length : 0;
// console.log(`love is ${count} times present`);

const sen = 'You cannot end a sentence with because because because is a conjunction';
const matches = sen.match(/because/gi);
const count = matches ? matches.length : 0;
console.log(`because is ${count} times present`);

//3 Pending
//4 Pending